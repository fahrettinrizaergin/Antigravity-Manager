# Dockerfile for Antigravity Tools Frontend (React/Vite)
#
# This Dockerfile builds the React frontend as a static site and serves it with nginx.
# The frontend is built using Vite and served as static files.
# This is suitable for web-based access to the Antigravity Tools interface.

FROM node:20-slim as builder

WORKDIR /app

# Copy package files
COPY package.json package-lock.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Build the frontend
RUN npm run build

# Production stage with nginx
FROM nginx:alpine

# Copy built files to nginx
COPY --from=builder /app/dist /usr/share/nginx/html

# Copy custom nginx configuration if needed
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
